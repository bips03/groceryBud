{
  "name": "backend",
  "version": "1.0.0",
  "description": "Express server for grocerybud frontend react",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "server": "nodemon index",
    "build" : "cd ../Frontend && npm run build",
    "install" : "cd ../Frontend && npm install",
    "heroku-postbuild" : "npm run install && npm run build"
  },
  "author": "Biplab",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.12.13"
  }
}
